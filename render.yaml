services:
  - type: web
    name: alpha-hackers-backend
    env: python
    plan: free
    rootDir: backend
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true